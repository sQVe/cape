{
  "eval_id": 2,
  "eval_name": "verbose-flag",
  "prompt": "Add a --verbose flag to our existing CLI tool. There are already --quiet and --json flags defined in src/cli.ts following the same pattern.",
  "assertions": [
    "Output uses inline mode — does NOT dispatch 3 divergent design agents for this trivial task",
    "Assumption challenge is brief (at most 2 rounds), appropriate for trivial scope",
    "Output contains a design summary (even if lightweight)"
  ]
}
