repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: prettier --write --ignore-unknown
        language: system
        types_or: [markdown]

      - id: beads
        name: beads sync
        entry: br sync --flush-only
        language: system
        pass_filenames: false
        always_run: true
