# Grove - Git worktree management
# https://github.com/sqve/grove

[preserve]
patterns = [".env", ".env.local", ".env.*.local", ".grove.toml"]

[hooks]
add = ["pnpm install"]

[autolock]
patterns = ["main"]
